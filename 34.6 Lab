#get 9 digit student number
num = int(input())

# extract the first, middle, and last parts of the number
#first 3 digits
first = str(num //1000000) 
#the middle 2 digits
middle = str(num //10000 % 100)
#last 4 digits
last = str(num % 10000)

#format the parts with hyphens and concatenate them
id_num = first + '-' + middle + '-' + last

# output the result
print(id_num)
